{
  "scripts": {
    "build": "esbuild --bundle --sourcemap --platform=node --target=node20 src/lambda.ts --outfile=build/lambda.js --jsx=automatic --external:'@aws-sdk/*'",
    "type-check": "tsc --project tsconfig.json --pretty --noEmit",
    "lint": "biome check --write ./"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.888.0",
    "aws-lambda": "^1.0.7",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "express-session": "^1.18.2",
    "http-status-codes": "^2.3.0",
    "openid-client": "^5.7.0",
    "serverless-http": "^4.0.0",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.4",
    "@tsconfig/recommended": "^1.0.10",
    "@types/aws-lambda": "^8.10.152",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/express-session": "^1.18.2",
    "@types/openid-client": "^3.7.0",
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2",
    "ultracite": "^5.3.4"
  }
}
